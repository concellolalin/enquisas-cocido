{% extends 'base.html.twig' %}

{% block title %}Opción edición{% endblock %}

{% block pagetitle %}Opción edición{% endblock %}

{% block body %}

    {{ form_start(edit_form) }}

        <div class="form-group">
            {{ form_label(edit_form.pregunta) }}
            {{ form_widget(edit_form.pregunta, { 'attr': {'class': 'form-control'} }) }}
        </div>

        <div class="form-group">
            {{ form_label(edit_form.valor) }}
            {{ form_widget(edit_form.valor, { 'attr': {'class': 'form-control'} }) }}
        </div>

        <div class="form-group">
            {{ form_label(edit_form.width) }}
            {{ form_widget(edit_form.width, { 'attr': {'class': 'form-control'} }) }}
        </div>

        <div class="form-group">
            {{ form_label(edit_form.height) }}
            {{ form_widget(edit_form.height, { 'attr': {'class': 'form-control'} }) }}
        </div>

        <div class="form-group">
            {{ form_label(edit_form.x) }}
            {{ form_widget(edit_form.x, { 'attr': {'class': 'form-control'} }) }}
        </div>

        <div class="form-group">
            {{ form_label(edit_form.y) }}
            {{ form_widget(edit_form.y, { 'attr': {'class': 'form-control'} }) }}
        </div>

        <input type="submit" value="Editar" class="btn btn-warning" />
    {{ form_end(edit_form) }}

    <br/>

    <div class="pull-right">
        {{ form_start(delete_form) }}
            <a href="{{ path('opcion_index') }}" class="btn btn-primary">Listado</a>
            &nbsp;&nbsp;&nbsp;
            <input type="submit" value="Eliminar" class="btn btn-danger" />
        {{ form_end(delete_form) }}
    </div>
{% endblock %}
